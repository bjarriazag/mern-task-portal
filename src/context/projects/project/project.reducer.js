const projectReducer = (state, action) => {
  switch (action.type) {
    case 'ADD_PROJECT':
      break;
    case 'EDIT_PROJECT':
      break;
    case 'DELETE_PROJECT':
      break;
    default:
      return state;
  }
};

export default projectReducer;
